<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title><%= painting.title %> Art Gallery</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <style>
         
        .col-md-6 .img-fluid {
            max-width: 75%;
            width: 75%;
            height: auto;
            max-height: none;
        }
    
    </style>

    <%- include('partials/navbar.ejs') %>
    <div class="container mt-4">
        <div class="row">
            <div class="col-md-6">
                <img src="<%= painting.image %>" class="img-fluid" alt="<%= painting.title %>">
            </div>
            <div class="col-md-6">
                <h1><%= painting.title %></h1>
                <h2>By <%= painting.artist %></h2>
                <p><%= painting.description %></p>
                <h3><strong>Price:$</strong><%= painting.price %></h3>
                <h4><strong>Category:</strong><%= painting.category %></h2>
                <h4><strong>Status</strong><%= painting.availability %></h4>
                <form action="/cart/add" method="POST">
                    <input type="hidden" name="id" value="<%= painting.paintingid %>">
                    <label for="quantity">Quantity:</label>
                    <input type="number" name="quantity" id="quantity" value="1" min="1">
                    <button type="submit">Add to Cart</button>
                </form>   
                <p>Follow the artist:</p>
                <% if (painting.socialmedia) { %>
                    <p><a href="<%= painting.socialmedia %>" target="_blank">Follow on Social Media</a></p>
                <% } %>
                <form action="/cart/add" method="post">
                    <input type="hidden" name="id" value="<%= painting.paintingid %>">
                    <button type="submit" class="btn btn-success">Add to Cart</button>
                </form>
                <a href="/" class="btn btn-secondary mt-3">Back to Gallery</a>
            </div>
        </div>
    </div>
</body>
</html>
